version: "2"

linters:
  default: standard # errcheck, govet, ineffassign, staticcheck, unused
  enable:
    - copyloopvar
    - misspell
    - unconvert
    - sqlclosecheck
    - bodyclose
    - nilerr

formatters:
  enable:
    - gofmt
    - goimports
