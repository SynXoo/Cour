---
description: Work on the next unchecked Phase 2 task from the ledger
---

Work on the next Phase 2 task. Open `docs/PHASE_2.md` → **Progress ledger**
and take the first unchecked task — unless a specific task is named here:
$ARGUMENTS

Follow the Session protocol in that file end to end: the linked milestone
section is the spec; implement, test, verify in the preview when it's UI,
check the box, append a Session log line, and commit as `M<x>.<y>: <summary>`.
Stay on the one task — new ideas go to the Parking lot.
